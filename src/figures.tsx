import React from 'react';
import ReactDOM from 'react-dom/client';
import JobsRoleEntryPage from './pages/JobsRoleEntryPage';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <JobsRoleEntryPage />
  </React.StrictMode>
);
